PORT=3000
SESSION_SECRET=replace-this-with-a-long-random-string
# For local dev this is ignored; firebase.js falls back to ./serviceAccountKey.json
# On Vercel, paste the ENTIRE serviceAccountKey.json contents as a single-line JSON string:
FIREBASE_SERVICE_ACCOUNT=
NODE_ENV=development
